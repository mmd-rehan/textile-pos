# MariaDB container configuration consumed by docker/docker-compose.yml.
# scripts/setup.sh writes the live values to docker/.env (gitignored).
MYSQL_DATABASE=textile_pos
MYSQL_USER=textile_pos
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_PORT=3306
